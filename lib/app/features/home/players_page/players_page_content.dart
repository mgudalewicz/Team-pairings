import 'package:flutter/material.dart';


class PlayersPageContent extends StatelessWidget {
  const PlayersPageContent({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Jeden'),
    );
  }
}
